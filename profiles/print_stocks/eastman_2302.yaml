print_stock_id: eastman_2302
print_stock_name: Eastman Color Print 5381
# Classic 1970s–80s theatrical print stock era. Higher contrast, warmer orange base
# density, slightly faded dye stability (period-accurate), rich deep blacks.
# The look of classic Scorsese, Coppola, Kubrick — Taxi Driver, The Godfather era.
# Note: Eastman Color prints from this era are also known for gradual color fading —
# the cyan dye fades first, shifting towards warm orange-red over time.

tone:
  shadow_lift: 0.018          # Vintage D-min
  contrast_boost: 1.02        # Small global punch on top of the per-channel print curve
  highlight_rolloff: 0.72     # Earlier rolloff — old prints compressed highlights faster
  highlight_rolloff_rate: 2.0 # Medium rolloff rate
  toe_depth: 0.90             # Deep shadow density
  # Softer vintage per-channel curve: strongly warm highlights, gently warm shadows.
  print_toe: 0.18
  print_shoulder: 0.18
  channel_toe_mult: [1.04, 1.0, 0.98]
  channel_shoulder_mult: [1.06, 1.0, 0.85]

color:
  shadow_bias_lab: [0.6, 0.2, 0.4]    # Gently warm-faded shadows (vintage lift, not orange-brown mud)
  midtone_bias_lab: [0.0, 0.7, 1.6]   # Golden-orange Eastman midtones (much gentler)
  highlight_bias_lab: [0.0, 0.4, 1.0] # Warm vintage highlights
  blue_suppression: 0.10               # Modest blue trim (was 0.32 — the ochre culprit)
  red_boost: 0.08                      # Vintage warm reds
  green_shift: 0.05                    # Yellow-green foliage shift
  saturation_scale: 1.02               # Slightly warm-biased print saturation

grain:
  strength: 0.10              # Slightly more prominent vintage print grain
  size: 0.42
